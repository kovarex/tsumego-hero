# Pending changelog entries

Fragments here describe changes that are not yet merged to master. They are
excluded from the generated manifest until their feature branch lands, so the
What's new page never advertises unfinished work.

When a change is merged, move its fragment back to `changelog/` and regenerate
the manifest. The `commit:` in the fragment should then resolve on master and
give the correct ship date.
