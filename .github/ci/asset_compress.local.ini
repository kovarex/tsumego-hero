; CI overrides for asset_compress.ini
; Copied to src/Config/asset_compress.local.ini during CI workflow

[General]
; Enable controller for on-the-fly asset generation during tests
; Required when debug=2 (debug mode enabled)
alwaysEnableController = true

; Don't cache config (reload on file changes)
cacheConfig = false
